// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String    @id @default(uuid())
  username  String    @unique
  password  String
  role      String    @default("AGENT") // Can be "ADMIN" or "AGENT"
  createdAt DateTime  @default(now())
  lastSeen  DateTime  @default(now())
  callLogs  CallLog[]
  sessions  AgentSession[]
}

model AgentSession {
  id        String   @id @default(uuid())
  agentId   String
  agent     User     @relation(fields: [agentId], references: [id], onDelete: Cascade)
  startTime DateTime @default(now())
  endTime   DateTime @default(now())
}

model CallLog {
  id          Int      @id @default(autoincrement())
  phoneNumber String
  type             String   // "INCOMING", "OUTGOING", "MISSED", "REJECTED"
  duration         Int      // actual talk duration in seconds
  ringingDuration  Int      @default(0) // ringing time in seconds
  timestamp        DateTime
  agentId     String
  agent       User     @relation(fields: [agentId], references: [id])
  createdAt   DateTime @default(now())

  @@unique([agentId, phoneNumber, type, timestamp])
}
